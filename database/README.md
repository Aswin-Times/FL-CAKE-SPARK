# @cake-spark/database

Prisma ORM schema, migration history, seed data, and database maintenance scripts for PostgreSQL.

## Commands

```bash
# Generate Prisma Client
pnpm db:generate

# Run schema migrations in development
pnpm db:migrate

# Apply migrations in production
pnpm db:migrate:deploy

# Execute seeds
pnpm db:seed
```
