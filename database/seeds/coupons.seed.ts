import { PrismaClient } from "@prisma/client";

export async function seedCoupons(prisma: PrismaClient) {
  console.info("Seeding coupons placeholder...");
  // Business seeding will be implemented in subsequent database phases
}
