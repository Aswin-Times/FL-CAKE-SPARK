// Re-export shared types directly from @cake-spark/shared to prevent duplication
export * from "@cake-spark/shared";

// Frontend-specific UI component types
export type ModalState = "open" | "closed";
