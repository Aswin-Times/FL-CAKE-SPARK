# @cake-spark/shared

Single source of truth for:

- API contracts
- Shared TypeScript types
- Zod validation schemas
- Enums and system constants

Shared across `@cake-spark/frontend` and `@cake-spark/backend`.
